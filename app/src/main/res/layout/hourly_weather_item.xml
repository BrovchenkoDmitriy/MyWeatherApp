<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_dataTime"
        android:layout_width="47dp"
        android:layout_height="47dp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/iv_item_weather_icon"
        app:layout_constraintStart_toStartOf="@+id/iv_item_weather_icon"
        app:layout_constraintTop_toBottomOf="@+id/iv_item_weather_icon"
        tools:text="Сегодня" />

    <ImageView
        android:id="@+id/iv_item_weather_icon"
        android:layout_width="47dp"
        android:layout_height="47dp"

        android:contentDescription="@string/iv_item_weather_icon"
        app:layout_constraintEnd_toEndOf="@+id/tv_item_day_temp"
        app:layout_constraintStart_toStartOf="@+id/tv_item_day_temp"
        app:layout_constraintTop_toBottomOf="@+id/tv_item_day_temp"
        tools:srcCompat="@tools:sample/backgrounds/scenic" />

    <TextView
        android:id="@+id/tv_item_day_temp"
        android:layout_width="47dp"
        android:layout_height="47dp"
        android:gravity="center"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="24" />

</androidx.constraintlayout.widget.ConstraintLayout>